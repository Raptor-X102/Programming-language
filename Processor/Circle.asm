; int main() {
PUSH 10
POP AX ; // RADIUS
PUSH AX
PUSH 2
MUL
POP BX ; // DIAMETR

PUSH AX
POP LX

DIV_2_LOOP:
PUSH LX
PUSH 2
SUB
POP LX
PUSH LX
PUSH 0
JA DIV_2_LOOP
PUSH LX
PUSH 0
JE ADD_HALF
JMP SKIP

ADD_HALF:
PUSH AX
PUSH 0.5
SUB
POP AX

SKIP:
PUSH 0
POP YX

Y_LOOP_START:
PUSH YX
PUSH BX
PUSH 0
POP XX
JA Y_LOOP_BREAK

	X_LOOP_START:
	PUSH XX
	PUSH BX
	JA X_LOOP_BREAK

	PUSH AX
	PUSH XX
	SUB
	PUSH AX
	PUSH XX
	SUB
	MUL

	PUSH AX
	PUSH YX
	SUB
	PUSH AX
	PUSH YX
	SUB
	MUL

	ADD
	POP FX

	PUSH AX
	PUSH AX
	MUL
	POP GX
	PUSH FX
	PUSH GX

	JBE BELONGS
	BELONGS_END:

	PUSH FX
	PUSH GX
	JA N_BELONGS
	N_BELONGS_END:

	PUSH XX
	PUSH 1
	ADD
	POP XX
	JMP X_LOOP_START

X_LOOP_BREAK:
PUSH YX
PUSH 1
ADD
POP YX
JMP Y_LOOP_START

Y_LOOP_BREAK:
DRAW 20 40
HLT

BELONGS:
PUSH BX
PUSH 2
MUL
PUSH YX
MUL
PUSH XX
PUSH 2
MUL
ADD
POP EX
PUSH 42
POP [EX]
PUSH 42
POP [EX+1]
JMP BELONGS_END

N_BELONGS:
PUSH BX
PUSH 2
MUL
PUSH YX
MUL
PUSH XX
PUSH 2
MUL
ADD
POP EX
PUSH 46
POP [EX]
PUSH 46
POP [EX+1]
JMP N_BELONGS_END
